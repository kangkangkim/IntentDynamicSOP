---
name: gc-phase-plan
capability_key: phase_planning
stage: planning
---

# GC Phase Plan

Enterprise atomic capability: execution phase decomposition and dependency DAG.

Produces:
- `<ENTERPRISE_PHASE_PLAN_REF>` — ordered phases with input/output contracts per phase
- `<ENTERPRISE_DEPENDENCY_DAG_REF>` — execution unit dependency graph

Input: tech_design output, task_contract
Output: gc_atomic_result

<!-- placeholder: real SOP content lives in the enterprise repository -->
